import 'zone.js';
// This file includes polyfills needed by Angular and is loaded before the app.
// You can add your own extra polyfills to this file.

/**
 * IE11 requires the following for NgClass support on SVG elements:
 */
// import 'classlist.js';  // Run `npm install --save classlist.js`.

/**
 * 
 * Web Animations `@angular/platform-browser/animations` will use this.
 * Needed for: All but the default Angular animations.
 */
// import 'web-animations-js';  // Run `npm install --save web-animations-js`.

/**
 * By default, Angular adds a polyfill for `intl` when running in the browser.
 * If you require support for additional locales, consider adding them here.
 */
// import 'intl';  // Run `npm install --save intl`.

// Import the following for compatibility with older browsers:
import 'core-js/es/array';
import 'core-js/es/number';
import 'core-js/es/object';
import 'core-js/es/string';
import 'core-js/es/symbol';
